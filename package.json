{
  "dependencies": {
    "@create-figma-plugin/utilities": "^1.8.2"
  },
  "devDependencies": {
    "@create-figma-plugin/build": "^1.8.2",
    "@create-figma-plugin/tsconfig": "^1.8.2",
    "@figma/plugin-typings": "1.40.0",
    "typescript": "^4"
  },
  "scripts": {
    "build": "build-figma-plugin --typecheck --minify",
    "watch": "build-figma-plugin --typecheck --watch"
  },
  "figma-plugin": {
    "editorType": [
      "figma"
    ],
    "id": "hello-world",
    "name": "Zero Frame",
    "menu": [
      {
        "name": "Create Zero Height Frame",
        "main": "src/create-zero-height-frame/main.ts",
        "parameters": [
          {
            "key": "length",
            "name": "Enter Frame width",
            "description": "Enter the desired width for your Zero Frame (defaults to 100px)",
            "optional": true
          }
        ]
      },
      {
        "name": "Create Zero Width Frame",
        "main": "src/create-zero-width-frame/main.ts",
        "parameters": [
          {
            "key": "length",
            "name": "Enter Frame height",
            "description": "Enter the desired height for your Zero Frame (defaults to 100px)",
            "optional": true
          }
        ]
      }
    ]
  }
}
